server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pedrodese/Delivery-System-Event-Driven-Microservices
          clone-on-start: true
          default-label: main
          search-paths: devops/config
